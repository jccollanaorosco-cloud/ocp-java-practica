package operators_and_statements._05_java_statements._01_if_then;

// ====== IF-THEN (haz algo SOLO si se cumple una condicion) ======
//
// Ejecuta un bloque solo si la condicion (un boolean) es true.
//   if (hourOfDay < 11) {
//       System.out.println("Buenos dias");
//   }
//
// Las llaves { } son opcionales si es UNA sola linea, pero es MEJOR ponerlas siempre.
//
// TRAMPA de sangria (indentacion): en Java los espacios/tabs NO cuentan.
//   if (hourOfDay < 11)
//       System.out.println("Buenos dias");
//       morningGreetingCount++;   // OJO: esto NO es parte del if!
//   Aunque este "sangrado", el ++ SIEMPRE se ejecuta, porque sin llaves el if
//   solo agarra la primera linea.
//
// Consejo: cuando veas un if en el examen, cuenta las llaves { } y OLVIDA la sangria.
//
// EN CORTO: if hace algo solo si la condicion es true; usa llaves para que quede claro
//           que lineas pertenecen al if.

public class IfThen {
}
