package operators_and_statements._06_advanced_flow_control._02_optional_labels;

// ====== ETIQUETAS (labels) ======
//
// Una etiqueta es un "nombre" que le pones a un bucle, para poder referirte a el.
// Se escribe con dos puntos (:) y por costumbre va en MAYUSCULAS.
//   OUTER_LOOP: for (int[] fila : matriz) {
//       INNER_LOOP: for (int i=0; i<fila.length; i++) {
//           ...
//       }
//   }
//
// Solas no hacen nada util; su poder aparece con break y continue en bucles anidados,
// porque te dejan decir A CUAL bucle quieres salir o saltar (no solo al mas cercano).
//
// EN CORTO: una etiqueta le pone nombre a un bucle para poder romper o saltar
//           ESE bucle en especifico (util cuando hay bucles anidados).

public class OptionalLabels {
}
